import fetch, { FETCHED_BATCHES } from './fetch'
import create, { CREATE_BATCH } from './create'
import fetchOne, { FETCHED_BATCH } from './fetchOne'
import createNewStudent, { CREATE_STUDENT_BATCH } from './createNewStudent'
import fetchRandomStudent, { FETCHED_RANDOM_STUDENT } from './fetchRandomStudent'

export {
  FETCHED_BATCHES,
  CREATE_BATCH,
  FETCHED_BATCH,
  CREATE_STUDENT_BATCH,
  FETCHED_RANDOM_STUDENT,
  fetchRandomStudent,
  createNewStudent,
  fetch,
  fetchOne,
  create,
}
